<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Welcome' %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .container {
      background: white;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
      max-width: 500px;
      width: 90%;
    }
    .profile {
      text-align: center;
    }
    h1 {
      color: #1f2937;
      margin-bottom: 20px;
      font-size: 28px;
    }
    img {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      border: 4px solid #667eea;
      margin-bottom: 20px;
      object-fit: cover;
    }
    .info {
      background: #f3f4f6;
      padding: 20px;
      border-radius: 8px;
      margin: 20px 0;
      text-align: left;
    }
    .info p {
      margin: 10px 0;
      color: #374151;
      font-size: 14px;
    }
    .info strong {
      color: #1f2937;
    }
    .button {
      display: inline-block;
      margin-top: 20px;
      padding: 12px 30px;
      background: #f44336;
      color: white;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 600;
      transition: background 0.3s;
      border: none;
      cursor: pointer;
      font-size: 16px;
    }
    .button:hover {
      background: #da190b;
    }
    .welcome-message {
      text-align: center;
      margin-bottom: 30px;
    }
    a.home-link {
      display: inline-block;
      margin-top: 10px;
      color: #667eea;
      text-decoration: none;
      font-size: 14px;
    }
    a.home-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="profile">
      <% if (user) { %>
        <h1>Welcome <%= user.name %>!</h1>
        <img src="<%= user.picture %>" alt="profile" onerror="this.onerror=null;this.src='https://via.placeholder.com/120?text=No+Image';">
        <div class="info">
          <p><strong>Email:</strong> <%= user.email %></p>
          <p><strong>User ID:</strong> <small><%= user.id %></small></p>
          <% if (user.picture) { %>
          <% } %>
        </div>
        <button class="button" onclick="window.location.href='/logout'">Logout</button>
        <a href="/profile" class="home-link">View JSON Profile</a>
      <% } else { %>
        <div class="welcome-message">
          <h1>Welcome to Google Auth</h1>
          <p>A secure authentication system powered by Google OAuth2</p>
        </div>
        <a href="/auth/google" class="button" style="background: #1f2937;">Login with Google</a>
      <% } %>
    </div>
  </div>
</body>
</html>
